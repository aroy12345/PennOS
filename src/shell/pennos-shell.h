/**
 * shell function to be called upon startup
 * @param argc TODO: does this need any args?
 * @param argv TODO
 * @return nonzero on error, `1` on success
*/
void pennos_shell(int argc, char* argv[]);